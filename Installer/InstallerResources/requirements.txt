flextoolslib==2026.5.4
fuzzywuzzy 					
Levenshtein 				
mixpanel 			    	
PyQt6==6.10.2
PyQt6-WebEngine==6.10.0
regex
pygetwindow
tomli_w
# AI Rule Studio module (google-genai serves the default Gemini provider; anthropic and openai serve the other selectable providers; keyring stores the API key in the OS credential vault)
anthropic==0.115.1
google-genai==2.10.0
openai==2.44.0
keyring==25.7.0
# Markdown renders the AI explanation (Markdown -> HTML) in the rule preview
Markdown==3.10.2
# Currently wildebeest doesn't work with Python 3.13
# echo wildebeest-nlp 

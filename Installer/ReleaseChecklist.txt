Do these things when making a new release (when a beta release is not already set up)
=====================================================================================
x-Update module version #s to X.X if they are lower. E.g. bring everything up to 3.6 that was 3.5.x
x-Finish code changes and commit
x-Edit CreateInstaller.bat and set FLEXTRANS_VERSION at the top to the new version #.
x-Edit FLExTrans-installer.nsi setting PRODUCT_VERSION to the new version #.
x-Also change VIProductVersion in the .nsi file  to the new version #. This has to always have the 3rd # so make it 0 if needed. E.g. 3.12.0.${BUILD_NUM}
x-Change Copyright date if necessary in the .nsi file. 
x-Edit Version.py in the FlexTools folder with the new version #s (Also for FLExTools) (this shows up in the title bar and About)
x-Change TeamCity 
 -Click on Build FLExTrans Installer
 -Click on Settings
 -Under Build Configurations, Build FLExTrans Installer, click Edit
 -Change artifact paths for the new version
 -Click Save
 -Also change the Build FLExTrans Installer-3.15.X builder in multiple places to use for maintenance versions of 3.15
-Build and test the install locally
-Do another commit that has the install changes
-Finish merging all pull requests
-Test the install

Do these things when you are going live with the release
=========================================================
x-Make a new .download_info file changing the relevant data. 3 changes.
x-Upload the new .download_info file using FileZilla to the uploads_areas\flextrans folder (see SSH key ... in keepass for password)
x-Check in last changes
x-Create the release in Git with the new version string. 
 -Go to Code
 -Go to tags, switch to Releases
 -Click draft new release
 -Choose a tag (dropdown) and give new vx.x.x and click Create new tag xxx
 -Fill out Title (e.g. 3.7 - Sense Linker and Live Rule Tester Improvements)
 -Auto-generate release notes.
 -Publish
 -Edit release notes.
x-Update the user documentation
 -Create a subfolder for the old version (e.g. 3.13.1) in the FTP server's resources folder
 -Move UserDoc.htm to this folder
 -Move the Images folder to the new subfolder
 -Move UserDoc.htm in the beta folder up one level
 -Move the Images folder under beta up one level as well.
-If just doing a minor release without release notes and want to create a tag
 -Go to tags, switch to Releases
 -Click draft new release
 -Choose a tag (dropdown) and give new vx.x.xtag and click Create new tag xxx on publish
 -Publish, then delete the release, the tag will remain
x-Get FLExTransX.X.exe from the software.sil.org products folder and put in the local Installer folder
-Download zip files from TeamCity and put into git folders (main FT folder and Previous Versions)
x-Make a new post on software.sil.org/flextrans. (use same bullets as the Github release page)
x-Update the instructions on the download and installation web pages as necessary.
x-announce the new release on the FLEx list
-Send out the FLExTrans newsletter
-Setup things for the next release (so that new builds don't overwrite the just released one.)
 -See steps at the top

Switching to a newer version of Python
======================================
-Check compatibility with other tools, especially FlexTools and PyQt
-Download the Python installer something like: python-3.13.12-amd64.exe
-Copy this file to the local installer folder (the same path at the bottom of CreateInstaller.bat [e.g. -DRESOURCE_FOLDER=c:\data\FLExTrans\installer])
-Edit the BuildScripts\copyPythonBinary.ps1 and change the URL and PTH variables as appropriate
-Enable the BuildStep "Copy Python Binary" for the Build FLExTrans Installer build at least one time then you can disable it again.
-Edit the .nsi installer script with the new version (maybe in a couple places).
-Change the .vbs files in the InstallerResources\VBS folder to the updated version. E.g. PYTHON = "py -3.13"
-Change the command.bat (in InstallerResources) to the updated version. E.g. py -3.13
-Change the GitHub CI script to use py -3.xx instead of the existing version.

Switching to a newer version of PyQt
====================================
Update all the import statements that import the old PyQtX library
Update the unit test code, ci_stubs and test Rule assistant code for the same import issues
Change the convert convert_ui_py.bat to use the new version
Install what is needed for the new version of the Designer tool. For PyQt6 I used: pip install pyside6 and the executable was put in the python\...\scripts folder.
Change CreateInstaller.bat to have the new PyQt in the requirements text file for pip install